/**
 * @deprecated từ 0.19.0.
 * Huyền Học Các được lazy-mount trực tiếp trong CalendarView để không theo dõi/poll toàn DOM.
 * Giữ stub này để người dùng upload đè source qua GitHub Web không phải xóa file cũ thủ công.
 */
export function HuyenHocBridge() {
  return null;
}
